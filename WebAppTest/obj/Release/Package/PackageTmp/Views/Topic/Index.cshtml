@model IEnumerable<WebAppTest.Models.Topic>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Topic Data</h2>
<div class="x_content">
    <div class="row">
        <p>
            @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-secondary" })
        </p>
        <div class="card-box table-responsive">
            <table id="datatable" class="table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.TopicID)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.TopicName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Course.CourseName)
                        </th>
                        <th>Modify</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.TopicID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.TopicName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Course.CourseName)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.TopicID }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.TopicID }, new { onclick = "return confirm('The operation will delete data! You definitely want to delete this Topic?');" })
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
